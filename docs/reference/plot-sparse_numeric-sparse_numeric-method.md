# Plot two sparse_numeric vectors

Plot two sparse_numeric vectors

## Usage

``` r
# S4 method for class 'sparse_numeric,sparse_numeric'
plot(x, y, ...)
```

## Arguments

- x, y:

  `sparse_numeric` vectors of the same length.

- ...:

  Additional arguments (ignored).

# Euclidean norm of a sparse_numeric vector

Computes the Euclidean (L2) norm, i.e. \\sqrt(sum(x_i^2))\\.

## Usage

``` r
norm(x, ...)

# S4 method for class 'sparse_numeric'
norm(x, ...)
```

## Arguments

- x:

  A `sparse_numeric` object.

- ...:

  Ignored.

## Value

Numeric scalar norm.
